//user的vuex模块
const state={
  //存储数据
}
const mutations={
  //直接修改数据
}
const actions={
  //包含任意的异步操作，与用户做交互，间接提交mutations

}
const getters={
  //简化数据的操作
}
export default{
  state,
  mutations,
  actions,
  getters,
}